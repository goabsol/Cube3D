ray : y = mr * x + cr

sprite : y = -x / mc + cc

- cos(atan(-1/mc)) * 32 + cx

sin(atan(-1/mc)) * 32 + cy

mr = tan(rT)

mc = tan(pT) = (py + p.s / 2 - cy) / (px + p.s / 2 - cx)

cr = py + p.s / 2 - mr * (px + p.s /2)

cc = cx / mc + cy

x = (cc / mr - cr) / (mr + 1 / mc)

